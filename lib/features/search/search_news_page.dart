import 'package:flutter/material.dart';

class SearchNewsPage extends StatelessWidget {
  const SearchNewsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Search News Page"));
  }
}
